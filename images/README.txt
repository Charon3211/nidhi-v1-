Add Nidhi's profile photo here as `nidhi-profile.webp`.

The hero photo slot in `index.html` is marked CUSTOMIZE PHOTO. The original chat attachment is not available as a project file, so it must be copied here before it can be rendered on the live site.
